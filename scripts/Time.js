const UNIT_HOUR = "h";
const UNIT_MINUTE = "m";
const UNIT_SECOND = "s";

class Time {

  #milliseconds;

  constructor(milliseconds) {
    this.#milliseconds = milliseconds;
  }

  parseTime() {
    
  }
}